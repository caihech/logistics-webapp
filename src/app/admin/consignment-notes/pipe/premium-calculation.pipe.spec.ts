import { PremiumCalculationPipe } from './premium-calculation.pipe';

describe('PremiumCalculationPipe', () => {
  it('create an instance', () => {
    const pipe = new PremiumCalculationPipe();
    expect(pipe).toBeTruthy();
  });
});
